import React from "react";

class ResLocation extends React.Component{
    render(){
        return(
            <div>The Animal is found in {this.props.location}</div>
        )
    }
}

export default ResLocation;